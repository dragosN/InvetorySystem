global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Day 5: enable once orders-service exposes /metrics
  # - job_name: orders-service
  #   static_configs:
  #     - targets: ["host.docker.internal:8080"]

  # Day 5: enable once notifications-service exposes /metrics
  # - job_name: notifications-service
  #   static_configs:
  #     - targets: ["host.docker.internal:8081"]
